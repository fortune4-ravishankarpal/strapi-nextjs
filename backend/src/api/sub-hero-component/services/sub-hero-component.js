'use strict';

/**
 * sub-hero-component service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::sub-hero-component.sub-hero-component');
